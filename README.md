# quantics-image-compression
Python script for implementation of a tensor network method of image compression which emphasises the inter pixel patterns that repeat as the image is considered in varying grades of resolution.
